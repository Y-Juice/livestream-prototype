import SourceReader from '../components/SourceReader';

const Quran = () => {
  return (
    <SourceReader
      source="quran"
      sourceName="Quran"
      sourceDescription="The holy book of Islam, revealed to Prophet Muhammad (PBUH). Read and search through all 114 chapters (Surahs) with translations."
    />
  );
};

export default Quran; 